default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: db
  user: postgres
  password: <%= ENV['DB_PASSWORD'] %>

development:
  <<: *default
  database: rentalcars_development

 #test:
 # <<: *default
 # database: rentalcars_test

production:
  <<: *default
  database: rentalcars_production
  username: rentalcars
